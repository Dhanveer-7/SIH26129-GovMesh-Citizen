# Rural Development & Panchayat Raj Simulated API Configuration
PORT=3001

# Allowed origins for CORS (e.g. your GovMesh Core backend origin)
# Specify origins separated by commas (e.g. http://localhost:3000,http://localhost:8000)
# Leave empty to block external requests by default unless configured
GOVMESH_ALLOWED_ORIGIN=http://localhost:3000,http://localhost:8080
